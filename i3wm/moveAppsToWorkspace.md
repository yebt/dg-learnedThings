# Move all instances of a specific workspace with a command

This command move all instances of `Brave browser` to workspace 1

```bash
i3-msg [class="Brave-browser"] move container to workspace 1
```
